'use strict'

// const SteamConfig = require('./../src/index.js')
const name = 'SteamConfig.binaryKVtest'
const debug = require('ebug')(name)

// To keep Standard from complaining because of the IIFE..
; // eslint-disable-line

(function () {
  debug('Running %s', name)
}()
